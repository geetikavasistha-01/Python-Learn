a=(1,2,3,4)
print(type(a)) # a is a tuple

a=() #empty tuple
print(type(a)) 

a=(1,) # tuple with only one element
# tuples are immutable i.e they cannot be changed just like string but they are also like list 
# so they are lists but immutable
# it has() 